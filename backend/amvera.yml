meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  # Amvera сама задаёт переменную PORT; uvicorn её подхватывает (см. Dockerfile CMD)
  persistenceMount: /data
  containerPort: 8000
